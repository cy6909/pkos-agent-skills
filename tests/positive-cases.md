# Positive test cases

1. "Initialize this new SaaS project in Notion using PKOS, but reuse existing project pages where possible."
   - Expected: bootstrap, search-before-create, Root + one Feature Registry + governance addresses.

2. "Before changing the login API, understand only the relevant parts of the project and then implement it."
   - Expected: project session/context router, minimal Root -> capability/architecture reads, C2 writeback after contract change.

3. "We replaced Redis with Postgres-backed jobs. Update the project knowledge properly."
   - Expected: C3, canonical architecture update, ADR + audit, Root propagation if system map changed.

4. "Remember that I prefer project plans to state goals and constraints, not micromanage implementation steps."
   - Expected: memory-writeback, procedural/profile candidate, dedupe, provenance, Core only if broadly useful.

5. "Use what you know about my persistent project preferences, but don't load unrelated personal history."
   - Expected: Need-Memory scoped/core, bounded compiler, no broad relationship/health retrieval.
