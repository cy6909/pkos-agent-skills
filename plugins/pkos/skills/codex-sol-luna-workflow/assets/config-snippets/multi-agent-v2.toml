# Merge into ~/.codex/config.toml only when required by your Codex build.
# Do not replace unrelated existing configuration.

[features.multi_agent_v2]
hide_spawn_agent_metadata = false
tool_namespace = "agents"
