interface:
  display_name: "Codex Company Swarm"
  short_description: "Sol Max director, review board, paired dev/QA, and CI gates"
  default_prompt: "Use $codex-company-swarm in rapid-agile Sol Max mode. Keep this session as TD-01, create a managed company roster with a persistent Review Board Chair, pair every domain developer with an independent tester, require MFSQ security/performance coverage and authoritative CI evidence, bootstrap Jenkins only when no usable pipeline exists, integrate through one owner, and finish with a dashboard plus verified PKOS writeback."
policy:
  allow_implicit_invocation: false
