interface:
  display_name: "Codex Company Swarm"
  short_description: "Visible, routed, budgeted delivery with durable coordination"
  default_prompt: "Use $codex-company-swarm. This task is TD-01 planner and integrator. Prefer product delivery per Token, reuse visible tasks, cap active children at 6 and registered tasks at 8, maintain one cumulative candidate, verify on remote-12, and let PK-01 write Chinese Notion updates only at gates."
policy:
  allow_implicit_invocation: true
